<h5 class="question-title">
  <strong><%= question.escape("title") %></strong>
</h5>
<div class="question">
  <!-- Fill In Question with Question view -->
</div>

<div class="answers">
  <h5 class="answer-header">
    <%= _("Answer").pluralize(question.answers.length, true) %>
  </h5>
  <!-- Fill in with Answer view -->
</div>

<% if(!current_user || current_user.id != question.escape("user_id")) { %>
<h5 class="answer-title">Your Answer</h5>
<div class="new-answers-form">
  <!-- Fill in with answers form -->
</div>
<% } %>